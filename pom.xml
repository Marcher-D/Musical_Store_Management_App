<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.TamCa</groupId>
    <artifactId>musical-store-app</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <javafx.version>17.0.6</javafx.version>
        <!-- Thêm phiên bản MySQL Connector -->
        <mysql.connector.version>8.0.33</mysql.connector.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        
        <!-- THÊM MYSQL CONNECTOR J (Duy trì một bản duy nhất) -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>${mysql.connector.version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.10.1</version>
                <configuration>
                    <release>11</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>com.TamCa.store.App</mainClass>
                    
                    <vmArgs>
                        <vmArg>--add-exports=javafx.controls/com.sun.javafx.scene.control.skin=ALL-UNNAMED</vmArg>
                        <vmArg>--add-exports=javafx.base/com.sun.javafx.binding=ALL-UNNAMED</vmArg>
                        <vmArg>--add-exports=javafx.base/com.sun.javafx.collections=ALL-UNNAMED</vmArg>
                        
                        <vmArg>--add-opens=javafx.base/com.sun.javafx.reflect=ALL-UNNAMED</vmArg>
                        <vmArg>--add-opens=java.base/java.lang.reflect=ALL-UNNAMED</vmArg>
                    </vmArgs>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>